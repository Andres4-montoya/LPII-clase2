/*Tipos de datos primitivos JS
-->Number -> (Integers and Floats) 10 10.5 2000
   let numero = 10;
   let numeroDePuertas = 4;
   let numeroPi = 3.14;
   le valorMatricula = 2_000_000;
-->String -> (Cadena de caracteres)" A" 'B '
   let saludo = "Hola";
   let despido =" Chao";
   -->Boolean -> (true or false)
   let esCierto = false;
   let esFeo = true;
-->Object  -> ({} , nee Object() )
   let persona = {
    nombre :"Andres",
    apellido :"Montoya"
   };
   let empleado = {
    id=1, 
    nombre: "Fulano"
   };
-->Array   -> ( [])
   let arreglosObjetos = [persona,empleado];
-->BigInt -> (Valores numericos muy grandes)100n new BigInt(100)
   let numeroGrande = 100n;
   let numeroGrandeComoObjeto = new BigInt(100);
-->Undefined -> (Valor por defecto que tienen las variables si no son asignadas)
   let variableUndefined;
   let varibaleUndefined2 = undefined;
-->null -> (Se asigna quiero especificar que una varible esta vacía)
   let varaibleNula = null;
-->Symbol -> (Se utiliza para  crear valores únicos)
   let simbolo = new Symbol("Simbolo uno");
*/   